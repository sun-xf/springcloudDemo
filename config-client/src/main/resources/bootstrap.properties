server.port=7002
#{application}
spring.application.name=sun
#{profile}
spring.cloud.config.profile=test
#{label}
spring.cloud.config.label=master

#config server uri
spring.cloud.config.uri=http://localhost:7001/

#bootstrap.properties（bootstrap.yml）与application.properties（application.yml)执行顺序
#bootstrap.properties（bootstrap.yml）用来程序引导时执行，应用于更加早期配置信息读取，如可以使用来配置application.yml中使用到参数等
#application.properties（application.yml) 应用程序特有配置信息，可以用来配置后续各个模块中需使用的公共参数等。
#bootstrap.properties 先于 application.yml 加载